#include "main.h"

/**
 * _puts_recursion - prints a string usinng outs fn
 *
 * @s: the string variaable
 *
 * Return:Nothing
 */

void _puts_recursion(char *s)
{
	char str[20];

	strcpy(str, s);

	puts(str);
}
