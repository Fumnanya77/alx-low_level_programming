#include <stdio.h>
/**
 * main - Entry point
 *
 * Print: Print alphabet in lower case
 *
 * Return: Always 0 (Success)
 */

int main(void)
{
	char xter;

	for (xter = 'a'; xter <= 'z'; xter++)
	{
		putchar(xter);
	}
	putchar('\n');
	return (0);
}
