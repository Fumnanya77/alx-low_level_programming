#include "main.h"
#include <stdlib.h>

/**
 * _atoi - this function converts a string to an integer
 *
 * @s: The string variable to be converted
 */

int _atoi(char *s)
{
	int atoires;

	atoires = atoi(s);

	return (atoires);
}
