#include "main.h"
#include <stdlib.h>

/**
 * _abs - compute the absolute value of an integer
 *
 * @n: the parameter
 *
 * Return: An int, the absolut value
 */

int _abs(int n)
{
	int ab;

	ab = abs(n);

	return (ab);
}
