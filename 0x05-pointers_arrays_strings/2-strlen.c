#include <string.h>
#include "main.h"

/**
 * _strlen - Takes in pointer variable and counts the string length
 *
 * @s: the pointer variable
 *
 * Return: lenght of a string
 */

int _strlen(char *s)
{
	int lent;
	
	lent = strlen(s);

	return (lent);
}
